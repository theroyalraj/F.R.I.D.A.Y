// Service worker — extend for alarms / context menus → openclaw/trigger with secret from storage.
chrome.runtime.onInstalled.addListener(() => {
  console.info('[openclaw-ship] extension installed (skeleton)');
});
